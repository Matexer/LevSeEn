#include "Levenshtein.hpp"


using namespace std;


template<typename SizeT>
class Purify {
public:
    map<size_t, SizeT>* purify(map<size_t, SizeT>*, const SizeT patternLength) {
        auto tmp = new map<size_t, SizeT>;

    }
};